# 🐧 Comandos Linux — Terminal

## O que é um comando
- Comando = texto digitado no terminal que executa uma ação no sistema.

---

## Mudar diretório — `cd`

```bash
cd Documentos
cd ..
cd ~```

## Listar arquivos - `ls`
```ls
ls -l
ls -a
ls -la
ls -lh
ls -ltr```
